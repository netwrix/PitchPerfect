# OpenAI
OPENAI_API_KEY=sk-...

# Microsoft Entra OAuth
ENCRA_CLIENT_ID=your-entra-app-client-id
ENCRA_CLIENT_SECRET=your-entra-app-client-secret
ENCRA_TENANT_ID=your-entra-tenant-id

# Database (PostgreSQL URI, e.g. for Prisma or Sequelize)
DATABASE_URL=postgresql://user:password@host:port/dbname

# CORS / Frontend
FRONTEND_URL=https://your-frontend.onrender.com

# Sessions / Auth
SESSION_SECRET=generate-a-random-long-string

# Optional
LOG_LEVEL=info
NODE_ENV=production
# PORT is provided by Render automatically; use process.env.PORT in code
